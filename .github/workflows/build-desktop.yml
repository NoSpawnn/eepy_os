---
name: Build desktop container image
on:
  pull_request:
    branches:
      - main

  schedule:
    - cron: "05 10 * * *" # 10:05am UTC everyday

  push:
    branches:
      - main
    paths-ignore:
      - "**.md"
      - "server.containerfile"

  workflow_dispatch:

jobs:
  build_image:
    name: Build image
    uses: ${GITHUB_WORKSPACE}/.github/workflows/build-image.yml
    secrets: inherit
    strategy:
      fail-fast: true
      matrix:
        image_flavor: ["desktop"]
    with:
      image_flavor: ${{ matrix.image_flavor }}
